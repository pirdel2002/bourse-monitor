{
  "name": "bourse-signal-bot",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check public/app.js && node --test",
    "strategy:dry-run": "node scripts/reversal-dry-run.js"
  },
  "engines": {
    "node": ">=22.5"
  }
}
